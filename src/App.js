import React, { Component } from "react";

export default class App extends Component {
  render() {
    return (
      <div>
        <button> Usuário </button>
        <button> Maps </button>
        <button> Quiz </button>
        <button> Vídeo </button>
      </div>
    );
  }
}
